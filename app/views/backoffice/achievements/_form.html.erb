<%= form_with(model: achievement, local: true, url: path) do |form| %>
  <% if achievement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(achievement.errors.count, "error") %> prohibited this achievement from being saved:</h2>

      <ul>
      <% achievement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="field form-group col-md-6">
    <%= form.label "Descrição da conquista" %>
    <%= form.text_field :description, class: "form-control" %>
  </div>

  <div class="field form-group col-md-6">
    <%= form.label "Badge" %>
    <%= form.text_field :badge_path, class: "form-control"  %>
  </div>

  <div class="actions  col-md-12">
    <%= form.submit "Enviar", :class => "btn btn-default" %>
  </div>
</div>

<% end %>
