<%= form_with(model: weekly_challenge_bank_question, local: true, url: path) do |form| %>
  <% if weekly_challenge_bank_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weekly_challenge_bank_question.errors.count, "error") %> prohibited this weekly_challenge_bank_question from being saved:</h2>

      <ul>
      <% weekly_challenge_bank_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group col-md-6">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="field form-group col-md-6">
    <%= form.label "Nível" %>
    <%= collection_select(:weekly_challenge_bank_question, :level_id, Level.all, :id, :couse_level_title, {:prompt => true}, {:class=>'form-control'}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
