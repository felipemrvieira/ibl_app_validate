<%= form_with(model: student_book, local: true) do |form| %>
  <% if student_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_book.errors.count, "error") %> prohibited this student_book from being saved:</h2>

      <ul>
      <% student_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :current_page %>
    <%= form.text_field :current_page %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
