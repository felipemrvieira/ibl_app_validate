
<h1 class="page-header">Exams</h1>


    <div class="col-md-12">

        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>id</th>
                        <th>Titulo</th>
                        <th>Nível</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <% @exams.each do |exam| %>
                        <tr>
                            <td><%= exam.id %></td>
                            <td><%= exam.title %></td>
                            <td>
                             <% if exam.level %>
                                <%= exam.level.title %>
                            <% end %>
                            </td>
                            <td><%= link_to "baixar", exam.attachment_url, target: '_blank' %></td>
                            <td>
                                <%= link_to 'Editar', edit_backoffice_exam_path(exam), class: "btn btn-outline btn-primary" %>
                                <%= link_to 'Excluir', backoffice_exam_path(exam),  class: "btn btn-outline btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
                            </td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
            <div class="col-md-12">
                <div style="text-align: center;">      
                <%# pagy_bootstrap_nav(@pagy).html_safe %>
                </div>
            </div>  
        </div>
    </div>
