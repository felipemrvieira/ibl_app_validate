<%= form_with(model: exam, local: true, url: path) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% exam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="field form-group col-md-12">
    <%= form.label "Título do Simulado" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="field form-group col-md-6">
    <%= form.label "Exam" %>
    <%= form.file_field :attachment %>
  </div>

   <div class="field form-group col-md-6">
    <%= form.label "Nível" %>
    <%= collection_select(:exam, :level_id, Level.all, :id, :couse_level_title, {:prompt => true}, {:class=>'form-control'}) %>
  </div>

  <div class="actions  col-md-12">
    <%= form.submit "Enviar", :class => "btn btn-default" %>
  </div>
</div>

<% end %>
