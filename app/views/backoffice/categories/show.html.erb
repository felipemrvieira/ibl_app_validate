<h1 class="page-header">Categoria <%= @category.title %></h1>

<div class="col-md-6">
  <div class="panel panel-default ">
    <div class="panel-heading">
      Tag
    </div>
    <div class="panel-body">
      <p>
        <strong>Título:</strong>
        <%= @category.title %>
      </p>
      <%= link_to 'Editar', edit_backoffice_category_path(@category), class: "btn btn-outline btn-primary" %>
      <%= link_to 'Excluir', backoffice_category_path(@category),  class: "btn btn-outline btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>
  </div>
</div>




