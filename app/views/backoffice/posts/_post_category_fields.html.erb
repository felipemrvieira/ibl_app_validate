<div class="nested-fields">

  <div class="field form-group col-md-12">
    <%= f.label "Categoria" %>
    <% if @categories %>
      <% safe_categories = @categories %>
    <% else %>
      <% safe_categories = [] %>
    <% end %>
      <%= f.collection_select(:category_id, safe_categories, :id, :title, 
      { :prompt => 'Selecione a Categoria' }, {:class=>'form-control'} ) %>
  </div>
  
  <%
=begin%>
 <% if f.object.active == nil %>
   <%= f.hidden_field :active, :value => true %>
   <%= f.hidden_field :expiration, :value => Time.now + 1.year %> 
  <% end%> 
<%
=end%>

  
  <div class=" col-md-12">
    <%= link_to_remove_association "Remover Categoria", f %>
  </div>


</div>